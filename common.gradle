
ext.getDate = { ->
    def date = new Date()
    def formattedDate = date.format('yyyyMMdd', TimeZone.getTimeZone('UTC'))
    return formattedDate
}

ext.getDateTime = { ->
    def date = new Date()
    def formattedDate = date.format("yyyy-MM-dd'T'HH:mm:ss'Z'", TimeZone.getTimeZone('UTC'))
    return formattedDate
}

ext.getGitHash = { ->
    def stdout = new ByteArrayOutputStream()
    exec {
        commandLine 'git', 'rev-parse', '--short', 'HEAD'
        standardOutput = stdout
    }
    return stdout.toString().trim()
}

// Repeat compatibility sttings here for Eclipse Buildship
// Ref: https://github.com/eclipse-buildship/buildship/issues/978#issuecomment-616382644 
sourceCompatibility = '1.8'
targetCompatibility = '1.8'
